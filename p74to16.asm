DATA SEGMENT
SUM  DB ?
c16 db 16
hex db "0123456789ABCDEF"
DATA ENDS
CODE SEGMENT
ASSUME DS:DATA,CS:CODE
START:
MOV AX,DATA
MOV DS,AX
MOV CX,5
MOV SUM,0
ONE:
MOV AH,1
INT 21H
AND AL,0FH
ADD SUM,AL
LOOP ONE

MOV AX,DATA
MOV DS,AX
MOv CX,2
MOV Al,sum
TWO:
MOV AH,0
DIV C16
PUSH AX;余数在AH中压栈
LOOP TWO

MOV CX,2
LEA BX,HEX
THREE:
POP DX;弹出余数在DH中
XCHG DH,DL;余数在DL中
MOV AL,DL
XLAT
MOV DL,AL;结果在AL中
MOV AH,2
INT 21H;输出
LOOP THREE

MOV AX,4C00H
INT 21H
CODE ENDS
END START